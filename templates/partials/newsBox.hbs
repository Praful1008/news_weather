<style>
    .news-box {
        background-image: radial-gradient(rgba(255, 255, 255, 0.6), rgba(255, 255, 255, 0.8));
        color: black;
        text-align: center;
        width: 320px;
        height: 428px;
        overflow: hidden;
        margin: 16px 8px;
        display: block;
        box-sizing: border-box;
        border-radius: 8px;
        box-shadow: 2px 2px 10px rgb(76, 115, 173);
        padding-bottom: 10px;
        border: 2px solid transparent;
        position: relative;
    }

    .news-box:hover {
        border: 2px solid #114e9e;
    }

    .news-img {
        height: 200px;
        width: 94%;
    }

    .news-content {
        color: #0d4da0;
    }

    .news-box .title {
        color: rgb(41 148 124);
    }

    .time_stamp {
        display: block;
        width: 100%;
        position: absolute;
        background-color: rgb(48, 64, 102);
        color: #1be5bd;
        bottom: 0px;
    }

    .Author {
        color: #114e9e;
    }

</style>

<a class="news-box" href={{url}}>
    <img class="news-img" src={{urlToImage}} alt="images/weather.gif" onerror=this.src="images/breaking-news.gif">
    <span class="author">Author : {{#if author}}
        {{author}}
        {{else}}
        Anonymous
        {{/if}}</span>
    <h4 class="title">{{#if title}}
        {{title}}
        {{else}}
        Unknown Title
        {{/if}}
    </h4>
    <div class="news-content">
        <p>{{description}}</p>
    </div>
    <span class="time_stamp">{{publishedAt}}</span>
</a>